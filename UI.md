# UI 계획서

## 컨셉
Steel Ball Run의 '황금 회전(黄金の回転)' 미학.
어둡고 중후한 배경 위에 금색 액센트. 회전하는 iframe이 페이지의 중심이자 전부.

---

## 레이아웃 구조

```
┌─────────────────────────────────────┐
│           [HEADER 영역]              │
│    タイトル: 完全なる黄金の回転エネルギー  │
│    부제: Wanjeon Hoejeon             │
├─────────────────────────────────────┤
│           [CONTROL 영역]             │
│  ┌──────────────────────┐ [回転開始]  │
│  │  https://...         │           │
│  └──────────────────────┘           │
│   속도 슬라이더  ←────────→           │
├─────────────────────────────────────┤
│                                     │
│         [STAGE 영역]                 │
│                                     │
│      ┌───────────────────┐          │
│      │                   │          │
│      │   회전하는 iframe   │ ← 중앙 배치, 회전 중
│      │                   │          │
│      └───────────────────┘          │
│                                     │
└─────────────────────────────────────┘
```

---

## 영역별 상세

### HEADER
- 타이틀: `完全なる黄金の回転エネルギー`
  - 폰트: 세리프 계열 (Noto Serif JP 또는 Cinzel)
  - 색상: 금색 (`#D4AF37`) + 텍스트 글로우 효과
- 부제: `Wanjeon Hoejeon` 소문자, 작게

### CONTROL
- **URL 인풋**: 넓은 텍스트 인풋, 어두운 배경 + 금색 테두리
- **버튼**: "回転開始" (회전 개시)
  - 클릭 시 iframe에 URL 로드 + 회전 애니메이션 시작
- **속도 슬라이더**: 회전 속도 조절 (0.5s ~ 10s per revolution)
  - 기본값: 4s

### STAGE
- 배경: 짙은 네이비/검정 (`#0a0a0f`)
- iframe 컨테이너:
  - 크기: `60vmin × 60vmin` (정사각형, 뷰포트 기준)
  - 테두리: 금색 더블 보더 + 박스 섀도우 글로우
  - 회전: `@keyframes kaiten { from { transform: rotate(0deg) } to { transform: rotate(360deg) } }`
  - `animation-timing-function: linear` (등속 회전)
- iframe 자체: 컨테이너와 같은 크기, 회전 중에도 클릭/스크롤 가능

---

## 색상 팔레트
| 이름 | 코드 | 용도 |
|------|------|------|
| Golden | `#D4AF37` | 제목, 버튼, 테두리 액센트 |
| Deep Gold | `#8B6914` | 버튼 hover, 슬라이더 트랙 |
| Void | `#0a0a0f` | 배경 |
| Surface | `#12121a` | 카드/인풋 배경 |
| Text | `#e8e0cc` | 일반 텍스트 |

---

## 인터랙션 흐름
1. 페이지 진입 → 타이틀 페이드인 + 미세한 황금 파티클(선택)
2. URL 입력 후 버튼 클릭(또는 Enter)
3. iframe에 URL 로드
4. 회전 애니메이션 시작
5. 슬라이더로 실시간 속도 변경 가능
6. 버튼 재클릭 시 회전 중지 / 텍스트 "回転停止" 토글
